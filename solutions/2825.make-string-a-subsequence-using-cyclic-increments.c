#include <stdlib.h>
#include <stdbool.h>

// @leet start
bool canMakeSubsequence(char* str1, char* str2) {
  int *letterCount = (int *)malloc(26 * sizeof(int));
  for (int i = 0; str1[i] != '\0'; i++) {
    letterCount
  }
}
// @leet end
